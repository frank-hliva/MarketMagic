x - save musi preniest riadky na BE
    x - uploadDataTable vo FE
x - po save
    x - UploadTemplate.ExportedDataPath = savepath
- nejde nacitat ulozeny template aj s datami 
    - template a datove subory su dva rozne typy suborov
        - ak nacitame datovy subor do slotu pre template - system by nas mal upozornit na chybu
        - ak nacitame template do datoveho suboru - system by nas mal upozornit na chybu
- automaticky pridat novy riadok ak bola v predoslom vyplnena hodnota
    // Predpokladám, že viewModel.Cells je ObservableCollection<RowType>
    let addEmptyRowIfNeeded () =
        let lastRow = viewModel.Cells.[viewModel.Cells.Count - 1]
        if isRowFilled lastRow then
            viewModel.Cells.Add(RowType()) // prázdny riadok

    // Volaj túto funkciu pri zmene buniek v poslednom riadku
    viewModel.Cells.CollectionChanged.Add(fun _ -> addEmptyRowIfNeeded())
- mazanie riadkov
- farby
----------------------------------------------------------------------------------------------
- MarketMagic.jl ma tiez precitat port z config.toml
- moznost zavriet UploadTemplate.ExportedData
- po prvom nacitani appky (ak neexistuje config.toml) spravit setup zo zipu config.toml
----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
x - 1. urobit jeden typ so vsetkym
    x - jeden typ
    x - jedna funkcia co vsetko precita
    x - vsetky fieldy
x - 2. zabranit viditelnosti veci co nechcem exportovat
x - 3. skombinovať s testovacími dátami
    x - prejdeme cyklom riadky DataTable.cells
        x - prejdeme vsetky columny DataTable
            x - zistime ci key v DataTable.columns je aj v UploadDataTable.columns
                x - ziskame hodnoty
                    x - DataTable.cells[DataTable.columns[key]]
x - cells ako 2D array

- DataGrid
    - na koniec pridat prazdny riadok
    - mazanie riadkov

- ukladanie konfigu po kazdej zmene

- zatvaranie suboru

- toolbar
    x - button pre nacitanie loadUploadTemplate
        x - ikonka
        x - dialog
    x - button pre nacitanie addExportedData
    x - button pre nacitanie saveUploadTemplate
    - vymenit ikonky


------------------------------------------------------------------------------------------------------

- json nahradit MessagePackom